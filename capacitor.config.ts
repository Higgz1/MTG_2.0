import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'higgz.MTG2.dev',
  appName: 'MTG_2.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
